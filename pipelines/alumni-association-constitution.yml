---
template: markdown

repository:
  name: alumni-association-constitution
  github: github.com
  main_branch: main

resource_types:
- name: github-webhook
  defaults:
    resources:
      alumni-association-constitution: (( grab webhook_configuration ))
